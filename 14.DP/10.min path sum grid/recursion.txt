Ques: https://leetcode.com/problems/minimum-path-sum/description/

Code:

#include <bits/stdc++.h>
using namespace std;

int solve(int i, int j, vector<vector<int>> &grid)
{
    if (i == 0 && j == 0) return grid[0][0];
    if (i < 0 || j < 0) return 10000; // large number simulating infinity

    int up = grid[i][j] + solve(i - 1, j, grid);
    int left = grid[i][j] + solve(i, j - 1, grid);

    return min(up, left);
}

int minSumPath(vector<vector<int>> &grid) {
    int n = grid.size();
    int m = grid[0].size();
    return solve(n - 1, m - 1, grid);
}

int main()
{
    vector<vector<int>> grid = {
        {1, 3, 1},
        {1, 5, 1},
        {4, 2, 1}
    };

    int result = minSumPath(grid);
    cout << "Minimum Path Sum: " << result << endl;

    return 0;
}


🧪 Input
cpp
Copy
Edit
grid = {
    {1, 3, 1},
    {1, 5, 1},
    {4, 2, 1}
}
✅ Output
mathematica
Copy
Edit
Minimum Path Sum: 7
⏱️ Time Complexity
scss
Copy
Edit
O(2^(m + n))
Because from every cell, we branch into 2 recursive calls (up, left).

🗂️ Space Complexity
scss
Copy
Edit
O(m + n)
Due to recursion stack depth (at most m+n).
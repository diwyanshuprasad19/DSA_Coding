Ques: https://leetcode.com/problems/partition-equal-subset-sum/description/

Code:

#include <bits/stdc++.h>
using namespace std;

bool solve(int index, int target, vector<int> &arr) {
    if (target == 0) return true;
    if (index == 0) return arr[0] == target;

    bool notTake = solve(index - 1, target, arr);
    bool take = false;
    if (target >= arr[index])
        take = solve(index - 1, target - arr[index], arr);

    return take || notTake;
}

bool subsetSumToK(int n, int k, vector<int> &arr) {
    return solve(n - 1, k, arr);
}

int main() {
    vector<int> arr = {2, 3, 1, 5};
    int k = 4;
    cout << (subsetSumToK(arr.size(), k, arr) ? "true" : "false") << endl;
    return 0;
}

Sample Input/Output
🔸 Input:
cpp
Copy
Edit
arr = {2, 3, 1, 5}
k = 4
🔸 Output:
arduino
Copy
Edit
true
Explanation: Subset {3,1} or {2,1,1} sums to 4.

✅ Time and Space Complexity (Recursive Only)
Metric	Value
⏱ Time Complexity	O(2^n) — For each index, we either take or not take the current element
💾 Space Complexity	O(n) — Recursion stack depth
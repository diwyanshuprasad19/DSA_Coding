Ques:https://leetcode.com/problems/house-robber/description/

Code:

#include <bits/stdc++.h>
using namespace std;

int solve(int index, vector<int> n, vector<int> &dp)
{
    if (index == 0) return n[index];
    if (index < 0) return 0;
    if (dp[index] != -1) return dp[index];

    int pick = n[index] + solve(index - 2, n, dp);
    int notpick = 0 + solve(index - 1, n, dp);

    return dp[index] = max(pick, notpick);
}

int maximumNonAdjacentSum(vector<int> &nums){
    int k = nums.size();
    vector<int> dp(k, -1);
    int n = solve(k - 1, nums, dp);
    return n;
}

int main()
{
    vector<int> v{1, 2, 3, 4};
    int result = maximumNonAdjacentSum(v);
    cout << result;
    return 0;
}


🧪 Input
cpp
Copy
Edit
v = {1, 2, 3, 4}
✅ Output
Copy
Edit
6
⏱️ Time Complexity
scss
Copy
Edit
O(n)
Each index is solved once and stored in dp.

🗂️ Space Complexity
scss
Copy
Edit
O(n)`
dp[n] for memoization

O(n) recursion stack in the worst case


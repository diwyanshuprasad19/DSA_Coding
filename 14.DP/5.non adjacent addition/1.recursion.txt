Ques:https://leetcode.com/problems/house-robber/description/

Code:

#include <bits/stdc++.h>
using namespace std;

int solve(int index, vector<int> n)
{
    if (index == 0) return n[index];
    else if (index < 0) return 0;

    int pick = n[index] + solve(index - 2, n);
    int notpick = 0 + solve(index - 1, n);

    return max(pick, notpick);
}

int maximumNonAdjacentSum(vector<int> &nums){
    int k = nums.size();
    int m = solve(k - 1, nums);
    return m;
}

int main()
{
    vector<int> v{1, 2, 3, 4};
    int k = maximumNonAdjacentSum(v);
    cout << k;
    return 0;
}


🧪 Input
cpp
Copy
Edit
v = {1, 2, 3, 4}
✅ Output
Copy
Edit
6
⏱️ Time Complexity
scss
Copy
Edit
O(2^n)
Every index branches into 2 recursive calls → exponential time.

🗂️ Space Complexity
scss
Copy
Edit
O(n)
Max recursion stack depth is n (in worst case: every index - 1 call).
Ques:https://www.geeksforgeeks.org/greedy-algorithm-to-find-minimum-number-of-coins/

The goal of this code is to make the given string lexicographically smallest 
(i.e., alphabetically smallest) by swapping two distinct characters only once.

Code:

#include <bits/stdc++.h>
using namespace std;

int findMin(int n, vector<int> denomination)
{
    int count = 0; 

    // Traverse through all denomination
    for (int i = denomination.size() - 1; i >= 0; i--) {

        // Find denominations
        while (n >= denomination[i]) {
            n -= denomination[i];
            count++;
        }
    }
    return count; 
}

int main()
{
    vector<int> denomination = { 1, 2, 5, 10 };
    int n = 39;
    cout << findMin(n, denomination);
    return 0;
}
Sample Input
ini
Copy
Edit
denomination = [1, 2, 5, 10]
n = 39
Sample Output
Copy
Edit
6
(Explanation: 10+10+10+5+2+2)

Time Complexity
Let k = number of denominations.

The while loops run once per coin used. In the worst case (e.g., many 1s), that’s O(n) total iterations.

Outer loop over denominations is O(k), but dominated by the while work.

Overall (with pre-sorted denominations descending): O(n).
If you first sort the list, add O(k log k).

Space Complexity
Uses a few integers; no extra structures proportional to n or k.

O(1) auxiliary space.